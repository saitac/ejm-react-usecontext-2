import LoginButton from "./LoginButton";
import Panel from "./Panel";

const Form = () => {
    return(
        <Panel title="Bienvenido">
            <LoginButton/>
        </Panel>
    )
}

export default Form;